import createObservable from '../lib/observer'

export default function Home() {
  const observableFunction = createObservable<string>()

  return (
    <div>
      <main >
        <div>
        </div>
      </main>
    </div>
  )
}
